---
import CustomIcon from './CustomIcon.astro'
---

<aside class="not-content">
	<div>
		<p>
            This work is licensed under 
            <a href="http://creativecommons.org/licenses/by/4.0/?ref=chooser-v1" target="_blank" rel="license noopener noreferrer" style="display:inline-block;">
                CC BY 4.0&nbsp;
                <CustomIcon name="CC-1" size="1.2em"/>
                <CustomIcon name="CC-2" size="1.2em"/>
                <CustomIcon name="CC-3" size="1.2em"/>
                <CustomIcon name="CC-4" size="1.2em"/>
            </a>
        </p>
	</div>
</aside>

<style>
	aside {
		margin: 1rem 0;
		position: relative;
		/* background-color: var(--sl-color-gray-6); */
		padding: 1rem;
		border-radius: 0.5rem;
		display: flex;
		/* border: 1px solid var(--sl-color-text); */
		/* box-shadow: var(--sl-shadow-md); */
		overflow-y: hidden;
	}
    aside :global(:where(a)) {
		display: block;
		font-size: var(--sl-text-s);
		text-decoration: none;
		color: var(--sl-color-gray-3);
		overflow-wrap: anywhere;
	}
	aside :global(:where(a):hover) {
		color: var(--sl-color-white);
	}
	aside a::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: 1;
	}
	aside h2 {
		font-size: var(--sl-text-lg) !important;
	}
	aside p {
		font-size: var(--sl-text-sm) !important;
		line-height: normal;
	}
	aside > div:last-child {
		display: flex;
		align-items: end;
	}
	aside svg {
        /* margin-top: 0.3rem; */
		margin-bottom: 0rem;
        margin-right: 0.3rem;
		transition: transform 0.3s;
		transform: translateY(2px);
		/* width: 5.5rem; */
		height: auto;
	}

	aside:hover svg {
		transform: translateY(0px);
	}
</style>
